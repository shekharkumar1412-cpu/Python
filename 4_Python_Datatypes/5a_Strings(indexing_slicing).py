# string is a sequence of character enclosed in single/double quotes
# indexing/slicing in string is similar to list
# Python string are immutable(not changeable)
# we can iterate over striings using for loop similar to lists/tuples.
# To calculate length of a string we use len(string_name) function.


# declaring a string and assigning it to a variable
var='Shekhar_Kumar'

# using indexing and negative indexing concept to print a specific element
print(var[2])
print(var[-7])

print()

# using slicing to get a range of characters
print(var[:])
print(var[:8])
print(var[0:])


# mutliline string

msg='''
        Never give up.
        always keep smiling
    '''

print(msg)