# We can get the present working directory using the getcwd() method of the os module.

# This method returns the current working directory in the form of a string.

import os
print("OUTPUT is:")
print(os.getcwd())     # prints =>  D:\python\5_Python_files\Python Directory and Files Management
print("Output is above")
# Here, getcwd() returns the current directory in the form of a string.
#    (i.e the loctaion of the current file which is in Python Directory and Files Management folder 
#         which is a folder inside the folder 5_Python_files which is again a folder inside the 
#               folder python of windows D)
